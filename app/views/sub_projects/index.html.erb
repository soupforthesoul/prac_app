<h1>Listing projects</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Duration</th>
    <th>Startdate</th>
    <th>Enddate</th>
    <th>Priority</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sub_projects.each do |project| %>
  <tr>
    <td><%= sub_project.name %></td>
    <td><%= sub_project.description %></td>
    <td><%= sub_project.duration %></td>
    <td><%= sub_project.startdate %></td>
    <td><%= sub_project.enddate %></td>
    <td><%= sub_project.priority %></td>
    <td><%= link_to 'Show', sub_project %></td>
    <td><%= link_to 'Edit', edit_sub_project_path(project) %></td>
    <td><%= link_to 'Destroy', sub_project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Project', new_sub_project_path %>
